<table>
  <%= form_with model: @medical_department, url: admin_medical_department_path, local: true do |f| %>
    <tr>
      <th><%= f.label :名前 %></th>
      <td><%= f.text_field :name %></td>
    </tr>


   <tr>
      <td><%= f.submit %></td>
    </tr>
  <% end %>
</table>